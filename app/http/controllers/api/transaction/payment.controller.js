const Controller = require("../../controller");

class PaymentController extends Controller {
    createPaymentGatewayLink(req, res, next) {
        
    }
}

module.exports = new PaymentController();